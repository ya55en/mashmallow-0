#! /bin/sh
# mashmallow settings

# shellcheck disable=SC2034
{
    _BASHRCD="${MASH_BASHRCD:-$HOME/.bashrc.d}"
    _LOCAL="${MASH_LOCAL:-$HOME/.local}"

    _MASH_HOME="${MASH_HOME:-$_LOCAL/opt/mash}"
    _RECIPES_DIR="${_MASH_HOME}/share/recipes"
    _LIB_DIR="${_MASH_HOME}/lib"
}
